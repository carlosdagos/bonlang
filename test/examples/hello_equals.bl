---
output: "Testing this...\nIs equal? True\nWhat was equal... 2"
---

module Main where

def main [] = {
    puts-ln $ "Testing this...";

    val two = + $ 1 1;

    puts-ln $ (concat $ "Is equal? " (to-string $ (== $ 2 two)));

    puts $ (concat $ "What was equal... " (to-string $ two));
}
