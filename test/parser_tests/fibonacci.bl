module Main where

def fib [n] = if || $ (== $ n 0) (== $ n 1)
                 then 1
                 else + $ (fib $ (- $ n 1)) (fib $ (- $ n 2))

def main [] = {
    val fib6 = fib $ 6;
    puts-ln $ fib6;
}
